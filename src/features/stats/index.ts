export { default as StatsContent } from './StatsContent.vue';
export { default as StatsControls } from './StatsControls.vue';

export function initStatsFeature() {
  // Vue-native stats are mounted by App.vue.
}
