export { default as CoupleFeature } from './CoupleFeature.vue';
export { coupleFeature, refreshCouple, showCouple } from './feature.js';

export function initCoupleFeature() {
  // Couple logic is still hosted by the legacy app bundle during phase 1.
}
